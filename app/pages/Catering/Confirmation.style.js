import {StyleSheet } from 'react-native';

export default StyleSheet.create({
    infoText: {
        fontSize: 17,
        fontWeight: 'bold',
    },
    infoBoxForLine: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        margin: 5,
        padding: 7,
    }
});